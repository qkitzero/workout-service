name: Add PR to project

on:
  pull_request_target:
    types:
      - opened
      - ready_for_review
      - converted_to_draft

permissions: {}

jobs:
  add-to-project:
    if: github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    steps:
      - uses: actions/add-to-project@v1.0.2
        id: add-project
        with:
          project-url: https://github.com/users/qkitzero/projects/1
          github-token: ${{ secrets.ADD_TO_PROJECT_PAT }}

      - env:
          GH_TOKEN: ${{ secrets.ADD_TO_PROJECT_PAT }}
          PR_ACTION: ${{ github.event.action }}
          PR_DRAFT: ${{ github.event.pull_request.draft }}
        run: |
          IN_PROGRESS="47fc9ee4"
          IN_REVIEW="e99e546b"

          if [ "$PR_ACTION" = "ready_for_review" ]; then
            STATUS_ID="$IN_REVIEW"
          elif [ "$PR_ACTION" = "converted_to_draft" ]; then
            STATUS_ID="$IN_PROGRESS"
          elif [ "$PR_DRAFT" = "true" ]; then
            STATUS_ID="$IN_PROGRESS"
          else
            STATUS_ID="$IN_REVIEW"
          fi

          gh api graphql -f query='
            mutation($projectId: ID!, $itemId: ID!, $fieldId: ID!, $optionId: String!) {
              updateProjectV2ItemFieldValue(
                input: {
                  projectId: $projectId
                  itemId: $itemId
                  fieldId: $fieldId
                  value: { singleSelectOptionId: $optionId }
                }
              ) {
                projectV2Item { id }
              }
            }' \
            -f projectId="PVT_kwHOCMKcgM4BQaRO" \
            -f itemId="${{ steps.add-project.outputs.itemId }}" \
            -f fieldId="PVTSSF_lAHOCMKcgM4BQaROzg-ic8c" \
            -f optionId="$STATUS_ID"
